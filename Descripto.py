import Alfabeto

def PalavraCriptografada(palavraCriptografada):
    palavraCriptografada = palavraCriptografada

def Chave(chave):
    partes = chave.split('/')
    codigo = partes[0]
    determinante = int(partes[1])

    verificarChave(codigo)
def verificarChave(codigo):

    if codigo in Alfabeto.codigos0:
        print("deu bom")